name: CD-Dev
on:
  push:
    branches: ["main"]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Dev (stub)
        run: echo "Deploy to dev - implement steps"
